import { css } from "@root/styled-system/css";

export default {
  root: css({ display: "flex", justifyContent: "space-between", alignItems: "center" }),

  label: css({ fontWeight: "bold" }),

  input: css({ width: "16", textAlign: "center" }),
};
