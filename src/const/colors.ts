export const fallbackColor = "#000000";

export const regexHexColor = new RegExp(/^#(?:[0-9a-fA-F]{3}){1,2}$/);

export const regexChannelStartWithZero = new RegExp(/^0.*/);

export const regexColorChannel = new RegExp(/^[0-9]+$/);
